/**
  Input maze object and


  solve () Returns the Boolean value of the statement “the maze is navigable, having a legal travel path that is continuous from start point to existing treasure”

  AKA

  False -> if its not possible to solve the maze

  True -> if it is possible to solve the maze
**/
public class MazeSolver {

    private Maze curLoaded;

    public MazeSolver(Maze maze) {
        curLoaded = new Maze(maze);
    }

    /**
      solve!
     **/
    public boolean solve() {
        return true;
    }
}
